<?xml version="1.0" encoding="UTF-8"?>
<completions>
    <set name="php.core.functions.password">
        <completion string="password_algos">
            <behavior symbol="function">
                <append>()</append>
            </behavior>
        </completion>
        <completion string="password_get_info">
            <behavior symbol="function">
                <append>($[$hash])</append>
            </behavior>
        </completion>
        <completion string="password_hash">
            <behavior symbol="function">
                <append>($[$password], $[$algo])</append>
            </behavior>
        </completion>
        <completion string="password_needs_rehash">
            <behavior symbol="function">
                <append>($[$hash], $[$algo])</append>
            </behavior>
        </completion>
        <completion string="password_verify">
            <behavior symbol="function">
                <append>($[$password], $[$hash])</append>
            </behavior>
        </completion>
    </set>
</completions>
